// Global authentication configuration
export const AUTH_CONFIG = {
  // Set to false to disable authentication enforcement
  // Set to true to enable authentication enforcement
  ENFORCE_AUTH: false,

  // Set to true to show login/logout in header even when auth is disabled
  SHOW_AUTH_UI: true,
};
